Todo
----

Create a player object class that will represent the user.

This should be done in conjunction with server side logic.

possible data that will be sent over the wire during game play:

coordinates (in world space)
Velocity (if the player is move, where are they moving from)
Direction Facing (which direction is the player facing)